<UserControl x:Class="SpaceWars.View.ControlsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpaceWars.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Back" HorizontalAlignment="Left" Margin="0,0,600,0" VerticalAlignment="Top"  Style="{StaticResource CustomFont}" FontSize="12" Background="#FFE81F" Width="50" BorderBrush="#FFFFE81F" Command="{Binding ButtonBack_Click}"/>
            <Label Content="Controls" HorizontalAlignment="Center" Margin="0,50,200,0" VerticalAlignment="Top" Foreground="#FFE81F" Style="{StaticResource CustomFont}" FontSize="40"/>
            <Image Width="500" Margin="0,100,0,0">
                <Image.Source>
                    <BitmapImage UriSource="/assets/images/Commands.png" />
                </Image.Source>
            </Image>
        </Grid>
    </Grid>
</UserControl>
